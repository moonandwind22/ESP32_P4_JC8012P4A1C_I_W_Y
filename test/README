Unit tests for protocol framing, snapshot adaptation, runtime Wi-Fi helpers
and Weather/TfL/News parsing helpers.

These sources were written for the PlatformIO test runner, which is no longer
used by the P4 firmware (now built with ESP-IDF). Include paths were updated
to point at `main/`. Wiring them into an ESP-IDF/Unity test project is future
work.
